{
  "name": "stealth-assist",
  "version": "1.0.0",
  "type": "module",
  "description": "Stealth Assist Extension",
  "scripts": {
    "build": "vite build",
    "build:firefox": "vite build && node scripts/patch-firefox-manifest.js",
    "watch": "vite build --watch",
    "test": "vitest run",
    "package:chrome": "npm run build && mkdir -p releases && cd dist && zip -r ../releases/stealth-assist-chrome.zip .",
    "package:firefox": "npm run build:firefox && mkdir -p releases && cd dist && zip -r ../releases/stealth-assist-firefox.zip ."
  },
  "dependencies": {
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "jsdom": "^25.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^2.0.0"
  }
}
