{
  "name": "stealth-assist",
  "version": "1.0.0",
  "type": "module",
  "description": "Stealth Assist Extension",
  "scripts": {
    "build": "vite build",
    "build:firefox": "vite build && node scripts/patch-firefox-manifest.js",
    "watch": "vite build --watch"
  },
  "dependencies": {
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
